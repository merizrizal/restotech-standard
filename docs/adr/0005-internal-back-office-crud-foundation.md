# Internal Back Office CRUD foundation

The Laravel package will include an internal reusable Back Office CRUD foundation instead of hand-building every CRUD controller or depending on a heavy admin package. The foundation will use minimal Blade components with a theme adapter system, initially implementing only a minimal theme and allowing future Bootstrap, AdminLTE, or custom adapters.
